# 本仓即构建上下文（release.sh 的 svc_context → panda-auth-admin），
# 故这些规则**相对仓根**，可直接生效。
#
# ⚠️ 一律带 `**/` 前缀：不带前缀的模式只匹配**上下文根**（Docker 的 dockerignore
#    不会像 gitignore 那样自动递归到任意深度），而实际产物在
#    src/PandaAuth.Admin/bin|obj —— 写成 `bin/` 等于没排除
#    （2026-09-16 实测：往 src/PandaAuth.Admin/bin/ 种入 20MB 后上下文涨到 20.45MB）。
#
# 规则清单与 me / server 的 Dockerfile.dockerignore 保持同一套（按各自的上下文形态改写），
# 判据：种入全套脏产物后，上下文里不含清单中任何一项。
**/bin/
**/obj/
**/TestResults/
**/node_modules/
# frontend/dist/ 见 .gitignore；今天 vite 的 outDir 已指向 wwwroot/admin，此条属
# 空转，但留着防「有人改回默认 outDir」时的潜在漂移（me / server 早已有这条）。
**/dist/
.git/
.vs/
.vscode/
.idea/
.zcode/
.superpowers/

# 敏感文件：本仓 .gitignore 早已声明这些路径，本份 .dockerignore 直到 2026-09-16 才补上
# （此前只排了构建产物）。
**/*.user
**/appsettings.Local.json
**/appsettings.*.Local.json
**/.env
**/*.env

# 本仓无 deploy/ 目录，这条属预防性规则（与 me / server 那份同一清单）。
# 也因为没有 deploy/，故不需要 server 那份的 `!**/deploy/.env.example` 否定规则。
**/deploy/backups/

# 原基线里的 `*.md` 已删除：不带 `**/` 的模式只匹配**上下文根**，它把本仓 5 个
# 随源码提交的文档一起挡掉了（README.md / README.en.md / CONTRIBUTING.md /
# LICENSING.md / SECURITY.md —— 2026-09-16 git ls-files 交叉验证实测）。
# 排除 .md 本就无收益（没有任何构建步骤读它），且 me / server 的
# Dockerfile.dockerignore 一直刻意不排 *.md，删掉后四处策略一致。

# 前端构建产物（由 vite 输出，不入库）。
# 不变式：git ls-files src/PandaAuth.Admin/wwwroot 命中数为 0，
# 该目录 100% 是构建输出。若将来这里混入**随源码提交**的资产
# （对照 server / website 的 wwwroot/brand/**），必须收窄为只排除
# src/PandaAuth.Admin/wwwroot/admin/，否则会把该资产挡在镜像外。
src/PandaAuth.Admin/wwwroot/
